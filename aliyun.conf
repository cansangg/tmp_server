; 告诉 stunnel 我们是客户端模式
client = yes
; 进程文件位置（随便放个临时目录）
pid = /tmp/stunnel.pid
; 前台运行，方便看日志（不加这个默认后台运行）
foreground = yes

; 定义一个专门针对阿里云的任务
[dashscope-proxy]
; 在本地的 8080 端口接客
accept = 127.0.0.1:8080
; 接到客之后，加密送往阿里云的 443 端口
connect = dashscope.aliyuncs.com:443